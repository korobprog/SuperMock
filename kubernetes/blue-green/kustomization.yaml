apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: supermock

resources:
  - backend-blue.yaml
  - backend-green.yaml
  - service.yaml
  - configmap.yaml

commonLabels:
  app.kubernetes.io/name: supermock
  app.kubernetes.io/component: backend
  app.kubernetes.io/part-of: supermock-platform
  app.kubernetes.io/managed-by: kustomize
  app.kubernetes.io/created-by: devops-team
# Патчи для настройки специфичных параметров окружения
# могут быть добавлены здесь при необходимости
# patchesStrategicMerge:
#  - patches/memory-limits.yaml
