# coturn env example

# Your TURN realm (usually your domain)
TURN_REALM=turn.example.com

# Long random hex secret used for token auth (use: openssl rand -hex 32)
TURN_AUTH_SECRET=change_me_long_hex

# If you prefer static user/pass instead of secret-based tokens, you can switch to:
# (and adjust command in docker-compose)
# TURN_USERNAME=turn_user
# TURN_PASSWORD=turn_pass


