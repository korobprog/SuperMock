{
  "profession": "frontend-developer",
  "language": "es",
  "categories": [
    {
      "id": "interview-questions",
      "name": "Preguntas de entrevista",
      "count": 156,
      "icon": "💬",
      "color": "bg-blue-100 text-blue-800"
    },
    {
      "id": "technical-tasks",
      "name": "Tareas técnicas",
      "count": 89,
      "icon": "⚡",
      "color": "bg-green-100 text-green-800"
    },
    {
      "id": "system-design",
      "name": "Diseño de sistemas",
      "count": 34,
      "icon": "🏗️",
      "color": "bg-purple-100 text-purple-800"
    },
    {
      "id": "behavioral",
      "name": "Preguntas conductuales",
      "count": 67,
      "icon": "🧠",
      "color": "bg-orange-100 text-orange-800"
    },
    {
      "id": "algorithms",
      "name": "Algoritmos y estructuras de datos",
      "count": 123,
      "icon": "📊",
      "color": "bg-red-100 text-red-800"
    },
    {
      "id": "best-practices",
      "name": "Mejores prácticas",
      "count": 78,
      "icon": "⭐",
      "color": "bg-yellow-100 text-yellow-800"
    }
  ],
  "materials": [
    {
      "id": 1,
      "title": "Top 50 preguntas de JavaScript",
      "description": "Las preguntas más frecuentes en entrevistas de JavaScript con respuestas detalladas y ejemplos de código",
      "category": "interview-questions",
      "difficulty": "intermediate",
      "readTime": 15,
      "rating": 4.8,
      "reads": 1247,
      "tags": ["JavaScript", "Fundamentos", "ES6+", "Entrevista"],
      "content": "# Top 50 preguntas de JavaScript\n\n## 1. ¿Qué es hoisting en JavaScript?\n\nHoisting es un mecanismo de JavaScript donde las declaraciones de variables y funciones se elevan al inicio de su ámbito de visibilidad.\n\n```javascript\nconsole.log(x); // undefined\nvar x = 5;\n\n// Se interpreta como:\nvar x;\nconsole.log(x); // undefined\nx = 5;\n```\n\n## 2. Diferencia entre var, let y const\n\n- **var**: ámbito de función, se eleva (hoisting)\n- **let**: ámbito de bloque, no se eleva\n- **const**: ámbito de bloque, no se eleva, no se puede reasignar\n\n```javascript\n// var - ámbito de función\nfunction example() {\n  var x = 1;\n  if (true) {\n    var x = 2; // misma variable\n  }\n  console.log(x); // 2\n}\n\n// let - ámbito de bloque\nfunction example2() {\n  let x = 1;\n  if (true) {\n    let x = 2; // nueva variable\n  }\n  console.log(x); // 1\n}\n```\n\n## 3. ¿Qué son las clausuras (closures)?\n\nUna clausura es una función que tiene acceso a variables del ámbito exterior.\n\n```javascript\nfunction createCounter() {\n  let count = 0;\n  return function() {\n    return ++count;\n  };\n}\n\nconst counter = createCounter();\nconsole.log(counter()); // 1\nconsole.log(counter()); // 2\n```\n\n## 4. Event Loop en JavaScript\n\nEvent Loop es un mecanismo que permite a JavaScript ejecutar operaciones asíncronas.\n\n```javascript\nconsole.log('1');\nsetTimeout(() => console.log('2'), 0);\nPromise.resolve().then(() => console.log('3'));\nconsole.log('4');\n\n// Salida: 1, 4, 3, 2\n```\n\n## 5. Promise y async/await\n\n```javascript\n// Promise\nfunction fetchData() {\n  return new Promise((resolve, reject) => {\n    setTimeout(() => {\n      resolve('Datos obtenidos');\n    }, 1000);\n  });\n}\n\n// async/await\nasync function getData() {\n  try {\n    const data = await fetchData();\n    console.log(data);\n  } catch (error) {\n    console.error(error);\n  }\n}\n```\n\n## 6. Herencia prototipal\n\n```javascript\nfunction Animal(name) {\n  this.name = name;\n}\n\nAnimal.prototype.speak = function() {\n  console.log(`${this.name} hace un sonido`);\n};\n\nfunction Dog(name) {\n  Animal.call(this, name);\n}\n\nDog.prototype = Object.create(Animal.prototype);\nDog.prototype.constructor = Dog;\n\nconst dog = new Dog('Rex');\ndog.speak(); // Rex hace un sonido\n```\n\n## 7. Map vs Object\n\n```javascript\n// Object\nconst obj = {\n  key: 'value'\n};\n\n// Map\nconst map = new Map();\nmap.set('key', 'value');\nmap.set(1, 'number key');\nmap.set({}, 'object key');\n\nconsole.log(map.get('key')); // value\nconsole.log(map.size); // 3\n```\n\n## 8. Desestructuración\n\n```javascript\n// Arrays\nconst [a, b, ...rest] = [1, 2, 3, 4, 5];\nconsole.log(a, b, rest); // 1, 2, [3, 4, 5]\n\n// Objetos\nconst { name, age, ...other } = {\n  name: 'Juan',\n  age: 25,\n  city: 'Madrid',\n  job: 'Developer'\n};\nconsole.log(name, age, other); // Juan, 25, {city: 'Madrid', job: 'Developer'}\n```\n\n## 9. Módulos ES6\n\n```javascript\n// math.js\nexport const add = (a, b) => a + b;\nexport const subtract = (a, b) => a - b;\nexport default function multiply(a, b) {\n  return a * b;\n}\n\n// main.js\nimport multiply, { add, subtract } from './math.js';\nconsole.log(add(2, 3)); // 5\nconsole.log(multiply(2, 3)); // 6\n```\n\n## 10. Trabajo con DOM\n\n```javascript\n// Crear elemento\nconst div = document.createElement('div');\ndiv.textContent = '¡Hola mundo!';\ndiv.className = 'greeting';\n\ndocument.body.appendChild(div);\n\n// Buscar elementos\nconst element = document.querySelector('.greeting');\nconst elements = document.querySelectorAll('.item');\n\n// Manejadores de eventos\nconst button = document.querySelector('button');\nbutton.addEventListener('click', (event) => {\n  console.log('¡Botón presionado!');\n});\n```\n\nEstas son las preguntas básicas de JavaScript. Para una comprensión completa, se recomienda practicar con código y estudiar la documentación.",
      "isNew": false,
      "isPopular": true,
      "createdAt": "2024-01-01T00:00:00Z"
    },
    {
      "id": 2,
      "title": "React Hooks: guía completa",
      "description": "Análisis profundo de todos los hooks de React con ejemplos de uso y mejores prácticas",
      "category": "best-practices",
      "difficulty": "advanced",
      "readTime": 25,
      "rating": 4.9,
      "reads": 892,
      "tags": ["React", "Hooks", "Frontend", "useState", "useEffect"],
      "content": "# React Hooks: guía completa\n\n## Introducción\n\nReact Hooks fueron introducidos en React 16.8 y permiten usar estado y otras características de React en componentes funcionales.\n\n## useState\n\nHook básico para manejar estado.\n\n```jsx\nimport React, { useState } from 'react';\n\nfunction Counter() {\n  const [count, setCount] = useState(0);\n  \n  return (\n    <div>\n      <p>Contador: {count}</p>\n      <button onClick={() => setCount(count + 1)}>\n        Incrementar\n      </button>\n    </div>\n  );\n}\n```\n\n### Actualización funcional\n\n```jsx\nconst [count, setCount] = useState(0);\n\n// En lugar de esto:\nsetCount(count + 1);\n\n// Usa esto:\nsetCount(prevCount => prevCount + 1);\n```\n\n## useEffect\n\nHook para ejecutar efectos secundarios.\n\n```jsx\nimport React, { useState, useEffect } from 'react';\n\nfunction UserProfile({ userId }) {\n  const [user, setUser] = useState(null);\n  const [loading, setLoading] = useState(true);\n\n  useEffect(() => {\n    async function fetchUser() {\n      try {\n        const response = await fetch(`/api/users/${userId}`);\n        const userData = await response.json();\n        setUser(userData);\n      } catch (error) {\n        console.error('Error cargando usuario:', error);\n      } finally {\n        setLoading(false);\n      }\n    }\n\n    fetchUser();\n  }, [userId]); // Dependencias\n\n  if (loading) return <div>Cargando...</div>;\n  if (!user) return <div>Usuario no encontrado</div>;\n\n  return (\n    <div>\n      <h2>{user.name}</h2>\n      <p>{user.email}</p>\n    </div>\n  );\n}\n```\n\n### Limpieza de efectos\n\n```jsx\nuseEffect(() => {\n  const interval = setInterval(() => {\n    console.log('Tick');\n  }, 1000);\n\n  // Función de limpieza\n  return () => {\n    clearInterval(interval);\n  };\n}, []);\n```\n\n## useContext\n\nHook para trabajar con contexto.\n\n```jsx\n// Crear contexto\nconst ThemeContext = React.createContext();\n\n// Proveedor\nfunction App() {\n  const [theme, setTheme] = useState('light');\n\n  return (\n    <ThemeContext.Provider value={{ theme, setTheme }}>\n      <Header />\n      <Main />\n    </ThemeContext.Provider>\n  );\n}\n\n// Uso en componente\nfunction Header() {\n  const { theme, setTheme } = useContext(ThemeContext);\n\n  return (\n    <header className={`header-${theme}`}>\n      <button onClick={() => setTheme(theme === 'light' ? 'dark' : 'light')}>\n        Cambiar tema\n      </button>\n    </header>\n  );\n}\n```\n\n## useReducer\n\nHook para manejar estado complejo.\n\n```jsx\nimport React, { useReducer } from 'react';\n\nconst initialState = { count: 0 };\n\nfunction reducer(state, action) {\n  switch (action.type) {\n    case 'increment':\n      return { count: state.count + 1 };\n    case 'decrement':\n      return { count: state.count - 1 };\n    case 'reset':\n      return { count: 0 };\n    default:\n      throw new Error();\n  }\n}\n\nfunction Counter() {\n  const [state, dispatch] = useReducer(reducer, initialState);\n\n  return (\n    <div>\n      <p>Contador: {state.count}</p>\n      <button onClick={() => dispatch({ type: 'increment' })}>\n        +\n      </button>\n      <button onClick={() => dispatch({ type: 'decrement' })}>\n        -\n      </button>\n      <button onClick={() => dispatch({ type: 'reset' })}>\n        Reset\n      </button>\n    </div>\n  );\n}\n```\n\n## useCallback\n\nHook para memorizar funciones.\n\n```jsx\nimport React, { useState, useCallback } from 'react';\n\nfunction Parent() {\n  const [count, setCount] = useState(0);\n\n  const handleClick = useCallback(() => {\n    console.log('Botón presionado');\n  }, []); // Dependencias\n\n  return (\n    <div>\n      <p>Contador: {count}</p>\n      <button onClick={() => setCount(count + 1)}>\n        Incrementar contador\n      </button>\n      <Child onClick={handleClick} />\n    </div>\n  );\n}\n\nconst Child = React.memo(function Child({ onClick }) {\n  console.log('Child se re-renderizó');\n  return <button onClick={onClick}>Botón hijo</button>;\n});\n```\n\n## useMemo\n\nHook para memorizar valores.\n\n```jsx\nimport React, { useState, useMemo } from 'react';\n\nfunction ExpensiveComponent({ items }) {\n  const expensiveValue = useMemo(() => {\n    console.log('Calculando valor costoso');\n    return items.reduce((sum, item) => sum + item.value, 0);\n  }, [items]); // Dependencias\n\n  return <div>Resultado: {expensiveValue}</div>;\n}\n```\n\n## useRef\n\nHook para crear referencias mutables.\n\n```jsx\nimport React, { useRef, useEffect } from 'react';\n\nfunction TextInputWithFocusButton() {\n  const inputRef = useRef(null);\n\n  const focusInput = () => {\n    inputRef.current.focus();\n  };\n\n  return (\n    <div>\n      <input ref={inputRef} type=\"text\" />\n      <button onClick={focusInput}>Enfocar campo de entrada</button>\n    </div>\n  );\n}\n\n// Guardar valor anterior\nfunction usePrevious(value) {\n  const ref = useRef();\n  useEffect(() => {\n    ref.current = value;\n  });\n  return ref.current;\n}\n```\n\n## Hooks personalizados\n\n```jsx\n// useLocalStorage.js\nimport { useState, useEffect } from 'react';\n\nfunction useLocalStorage(key, initialValue) {\n  const [storedValue, setStoredValue] = useState(() => {\n    try {\n      const item = window.localStorage.getItem(key);\n      return item ? JSON.parse(item) : initialValue;\n    } catch (error) {\n      console.error(error);\n      return initialValue;\n    }\n  });\n\n  const setValue = value => {\n    try {\n      const valueToStore = value instanceof Function ? value(storedValue) : value;\n      setStoredValue(valueToStore);\n      window.localStorage.setItem(key, JSON.stringify(valueToStore));\n    } catch (error) {\n      console.error(error);\n    }\n  };\n\n  return [storedValue, setValue];\n}\n\n// Uso\nfunction App() {\n  const [name, setName] = useLocalStorage('name', 'Invitado');\n\n  return (\n    <div>\n      <input\n        value={name}\n        onChange={e => setName(e.target.value)}\n        placeholder=\"Ingresa tu nombre\"\n      />\n    </div>\n  );\n}\n```\n\n## Mejores prácticas\n\n1. **Siempre especifica las dependencias en useEffect**\n2. **Usa actualizaciones funcionales en useState**\n3. **Memoriza cálculos costosos con useMemo**\n4. **Memoriza funciones con useCallback**\n5. **Crea hooks personalizados para lógica reutilizable**\n6. **Evita crear objetos en el render**\n\n## Conclusión\n\nReact Hooks proporcionan una forma poderosa y flexible de manejar estado y efectos secundarios en componentes funcionales. El uso correcto de hooks lleva a código más limpio y mantenible.",
      "isNew": true,
      "isPopular": true,
      "createdAt": "2024-01-15T00:00:00Z"
    },
    {
      "id": 3,
      "title": "TypeScript: tipos avanzados",
      "description": "Estudio profundo del sistema de tipos de TypeScript con ejemplos prácticos",
      "category": "interview-questions",
      "difficulty": "advanced",
      "readTime": 18,
      "rating": 4.7,
      "reads": 234,
      "tags": ["TypeScript", "Tipado", "Frontend", "Genéricos"],
      "content": "# TypeScript: tipos avanzados\n\n## Introducción\n\nTypeScript proporciona un poderoso sistema de tipos que ayuda a escribir código más robusto. Veamos las capacidades avanzadas de tipado.\n\n## Tipos condicionales (Conditional Types)\n\n```typescript\ntype NonNullable<T> = T extends null | undefined ? never : T;\n\ntype T0 = NonNullable<string | number | null>; // string | number\ntype T1 = NonNullable<string[] | null | undefined>; // string[]\n```\n\n## Tipos mapeados (Mapped Types)\n\n```typescript\n// Hacer todas las propiedades opcionales\ntype Partial<T> = {\n  [P in keyof T]?: T[P];\n};\n\n// Hacer todas las propiedades requeridas\ntype Required<T> = {\n  [P in keyof T]-?: T[P];\n};\n\n// Hacer todas las propiedades de solo lectura\ntype Readonly<T> = {\n  readonly [P in keyof T]: T[P];\n};\n\n// Ejemplo de uso\ninterface User {\n  id: number;\n  name: string;\n  email?: string;\n}\n\ntype PartialUser = Partial<User>;\n// { id?: number; name?: string; email?: string }\n\ntype RequiredUser = Required<User>;\n// { id: number; name: string; email: string }\n```\n\n## Genéricos (Generics)\n\n```typescript\n// Genérico simple\nfunction identity<T>(arg: T): T {\n  return arg;\n}\n\nconst result = identity<string>('hello');\nconst result2 = identity('hello'); // inferencia automática de tipo\n\n// Genérico con restricciones\ninterface Lengthwise {\n  length: number;\n}\n\nfunction loggingIdentity<T extends Lengthwise>(arg: T): T {\n  console.log(arg.length);\n  return arg;\n}\n\nloggingIdentity('hello'); // OK\nloggingIdentity([1, 2, 3]); // OK\n// loggingIdentity(123); // Error\n```\n\n## Tipos utilitarios\n\n```typescript\n// Pick - seleccionar propiedades específicas\ninterface Todo {\n  title: string;\n  description: string;\n  completed: boolean;\n}\n\ntype TodoPreview = Pick<Todo, 'title' | 'completed'>;\n// { title: string; completed: boolean }\n\n// Omit - excluir propiedades específicas\ntype TodoInfo = Omit<Todo, 'completed'>;\n// { title: string; description: string }\n\n// Record - crear objeto con claves y valores específicos\ntype CatInfo = {\n  age: number;\n  breed: string;\n};\n\ntype CatName = 'miffy' | 'boris' | 'mordred';\n\nconst cats: Record<CatName, CatInfo> = {\n  miffy: { age: 10, breed: 'Persian' },\n  boris: { age: 5, breed: 'Maine Coon' },\n  mordred: { age: 16, breed: 'British Shorthair' }\n};\n```\n\n## Intersecciones y uniones de tipos\n\n```typescript\n// Unión de tipos\ntype Status = 'loading' | 'success' | 'error';\n\ntype ApiResponse<T> = {\n  data: T;\n  status: Status;\n};\n\n// Intersección de tipos\ninterface HasName {\n  name: string;\n}\n\ninterface HasAge {\n  age: number;\n}\n\ntype Person = HasName & HasAge;\n// { name: string; age: number }\n\n// Tipos condicionales con uniones\ntype ArrayElement<T> = T extends Array<infer U> ? U : never;\n\ntype T0 = ArrayElement<string[]>; // string\ntype T1 = ArrayElement<number[]>; // number\n```\n\n## Inferencia de tipos\n\n```typescript\n// Inferencia automática de tipo\nconst names = ['Alice', 'Bob', 'Charlie'];\n// TypeScript infiere automáticamente el tipo como string[]\n\n// Inferencia de tipo en funciones\nfunction map<T, U>(array: T[], fn: (item: T) => U): U[] {\n  return array.map(fn);\n}\n\nconst numbers = [1, 2, 3, 4];\nconst doubled = map(numbers, x => x * 2); // number[]\nconst strings = map(numbers, x => x.toString()); // string[]\n```\n\n## Template Literal Types\n\n```typescript\ntype EmailLocaleIDs = 'welcome_email' | 'email_heading';\ntype FooterLocaleIDs = 'footer_title' | 'footer_sendoff';\n\ntype AllLocaleIDs = `${EmailLocaleIDs | FooterLocaleIDs}_id`;\n// 'welcome_email_id' | 'email_heading_id' | 'footer_title_id' | 'footer_sendoff_id'\n\ntype Lang = 'en' | 'es';\ntype LocaleMessageIDs = `${Lang}_${AllLocaleIDs}`;\n// 'en_welcome_email_id' | 'en_email_heading_id' | ... | 'es_footer_sendoff_id'\n```\n\n## Tipos para componentes React\n\n```typescript\nimport React from 'react';\n\n// Tipado de props\ninterface ButtonProps {\n  children: React.ReactNode;\n  onClick?: () => void;\n  variant?: 'primary' | 'secondary' | 'danger';\n  size?: 'small' | 'medium' | 'large';\n  disabled?: boolean;\n}\n\nconst Button: React.FC<ButtonProps> = ({\n  children,\n  onClick,\n  variant = 'primary',\n  size = 'medium',\n  disabled = false\n}) => {\n  return (\n    <button\n      onClick={onClick}\n      className={`btn btn-${variant} btn-${size}`}\n      disabled={disabled}\n    >\n      {children}\n    </button>\n  );\n};\n\n// Tipado de hooks\nfunction useLocalStorage<T>(key: string, initialValue: T) {\n  const [storedValue, setStoredValue] = React.useState<T>(() => {\n    try {\n      const item = window.localStorage.getItem(key);\n      return item ? JSON.parse(item) : initialValue;\n    } catch (error) {\n      return initialValue;\n    }\n  });\n\n  const setValue = (value: T | ((val: T) => T)) => {\n    try {\n      const valueToStore = value instanceof Function ? value(storedValue) : value;\n      setStoredValue(valueToStore);\n      window.localStorage.setItem(key, JSON.stringify(valueToStore));\n    } catch (error) {\n      console.error(error);\n    }\n  };\n\n  return [storedValue, setValue] as const;\n}\n```\n\n## Tipos para API\n\n```typescript\n// Tipos para respuestas de API\ninterface ApiResponse<T> {\n  data: T;\n  status: 'success' | 'error';\n  message?: string;\n}\n\ninterface User {\n  id: number;\n  name: string;\n  email: string;\n}\n\n// Función de API tipada\nasync function fetchUser(id: number): Promise<ApiResponse<User>> {\n  const response = await fetch(`/api/users/${id}`);\n  return response.json();\n}\n\n// Uso\nconst userResponse = await fetchUser(1);\nif (userResponse.status === 'success') {\n  console.log(userResponse.data.name); // TypeScript sabe que es User\n}\n```\n\n## Conclusión\n\nLos tipos avanzados de TypeScript permiten crear tipos más expresivos y seguros. El uso correcto de estas capacidades lleva a mejor calidad de código y menos errores en tiempo de ejecución.",
      "isNew": true,
      "isPopular": false,
      "createdAt": "2024-01-20T00:00:00Z"
    }
  ]
}
