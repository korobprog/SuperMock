{
  "profession": "backend-developer",
  "language": "de",
  "categories": [
    {
      "id": "interview-questions",
      "name": "Vorstellungsgespräch Fragen",
      "count": 156,
      "icon": "💬",
      "color": "bg-blue-100 text-blue-800"
    },
    {
      "id": "technical-tasks",
      "name": "Technische Aufgaben",
      "count": 89,
      "icon": "⚡",
      "color": "bg-green-100 text-green-800"
    },
    {
      "id": "system-design",
      "name": "System Design",
      "count": 34,
      "icon": "🏗️",
      "color": "bg-purple-100 text-purple-800"
    },
    {
      "id": "behavioral",
      "name": "Verhaltensfragen",
      "count": 67,
      "icon": "🧠",
      "color": "bg-orange-100 text-orange-800"
    },
    {
      "id": "algorithms",
      "name": "Algorithmen & Datenstrukturen",
      "count": 123,
      "icon": "📊",
      "color": "bg-red-100 text-red-800"
    },
    {
      "id": "best-practices",
      "name": "Best Practices",
      "count": 78,
      "icon": "⭐",
      "color": "bg-yellow-100 text-yellow-800"
    }
  ],
  "materials": [
    {
      "id": 1,
      "title": "Node.js: Grundlagen und Architektur",
      "description": "Tiefgehende Analyse der Node.js-Architektur, Event Loop und grundlegende Prinzipien für Backend-Entwicklung",
      "category": "interview-questions",
      "difficulty": "intermediate",
      "readTime": 20,
      "rating": 4.8,
      "reads": 1156,
      "tags": ["Node.js", "JavaScript", "Backend", "Architektur"],
      "content": "# Node.js: Grundlagen und Architektur\n\n## Einführung\n\nNode.js ist eine JavaScript-Laufzeitumgebung, die auf der Chrome V8-Engine aufbaut und serverseitige JavaScript-Ausführung ermöglicht.\n\n## Node.js-Architektur\n\n```javascript\n// Event Loop Beispiel\nconsole.log('1');\nsetTimeout(() => console.log('2'), 0);\nsetImmediate(() => console.log('3'));\nprocess.nextTick(() => console.log('4'));\nconsole.log('5');\n\n// Ausgabe: 1, 5, 4, 2, 3\n```\n\n## Modulsystem\n\n```javascript\n// CommonJS\nconst fs = require('fs');\nconst path = require('path');\n\n// ES Modules\nimport fs from 'fs';\nimport path from 'path';\n\n// Modul erstellen\nmodule.exports = {\n  add: (a, b) => a + b,\n  subtract: (a, b) => a - b\n};\n```\n\n## Asynchrone Programmierung\n\n```javascript\n// Callbacks\nfs.readFile('datei.txt', 'utf8', (err, data) => {\n  if (err) {\n    console.error('Fehler:', err);\n    return;\n  }\n  console.log('Daten:', data);\n});\n\n// Promises\nconst readFilePromise = (filename) => {\n  return new Promise((resolve, reject) => {\n    fs.readFile(filename, 'utf8', (err, data) => {\n      if (err) reject(err);\n      else resolve(data);\n    });\n  });\n};\n\n// Async/Await\nasync function readFiles() {\n  try {\n    const data1 = await readFilePromise('datei1.txt');\n    const data2 = await readFilePromise('datei2.txt');\n    console.log(data1, data2);\n  } catch (error) {\n    console.error('Fehler:', error);\n  }\n}\n```\n\n## HTTP-Server\n\n```javascript\nconst http = require('http');\n\nconst server = http.createServer((req, res) => {\n  res.writeHead(200, { 'Content-Type': 'application/json' });\n  \n  const response = {\n    message: 'Hallo, Welt!',\n    timestamp: new Date().toISOString()\n  };\n  \n  res.end(JSON.stringify(response));\n});\n\nserver.listen(3000, () => {\n  console.log('Server läuft auf Port 3000');\n});\n```\n\n## Express.js Framework\n\n```javascript\nconst express = require('express');\nconst app = express();\n\n// Middleware\napp.use(express.json());\napp.use(express.urlencoded({ extended: true }));\n\n// Routen\napp.get('/api/benutzer', (req, res) => {\n  res.json({ benutzer: [] });\n});\n\napp.post('/api/benutzer', (req, res) => {\n  const { name, email } = req.body;\n  // Benutzer-Erstellungslogik\n  res.status(201).json({ message: 'Benutzer erstellt' });\n});\n\napp.listen(3000, () => {\n  console.log('Express-Server läuft');\n});\n```\n\n## Fehlerbehandlung\n\n```javascript\n// Globale Fehlerbehandler\nprocess.on('uncaughtException', (err) => {\n  console.error('Unbehandelte Ausnahme:', err);\n  process.exit(1);\n});\n\nprocess.on('unhandledRejection', (reason, promise) => {\n  console.error('Unbehandelte Promise-Ablehnung:', reason);\n});\n\n// Express-Fehlerbehandlungs-Middleware\napp.use((err, req, res, next) => {\n  console.error(err.stack);\n  res.status(500).json({ error: 'Etwas ist schiefgelaufen!' });\n});\n```\n\n## Best Practices\n\n### Sicherheit\n```javascript\n// Helmet für Sicherheits-Header verwenden\nconst helmet = require('helmet');\napp.use(helmet());\n\n// Eingabevalidierung\nconst { body, validationResult } = require('express-validator');\n\napp.post('/api/benutzer', [\n  body('email').isEmail(),\n  body('password').isLength({ min: 6 })\n], (req, res) => {\n  const errors = validationResult(req);\n  if (!errors.isEmpty()) {\n    return res.status(400).json({ errors: errors.array() });\n  }\n  // Gültige Daten verarbeiten\n});\n```\n\n### Performance\n```javascript\n// Streams für große Dateien verwenden\nconst fs = require('fs');\nconst readStream = fs.createReadStream('grosse-datei.txt');\nconst writeStream = fs.createWriteStream('ausgabe.txt');\n\nreadStream.pipe(writeStream);\n\n// Caching mit Redis\nconst redis = require('redis');\nconst client = redis.createClient();\n\nasync function getCachedData(key) {\n  const cached = await client.get(key);\n  if (cached) return JSON.parse(cached);\n  \n  const data = await fetchDataFromDatabase();\n  await client.setex(key, 3600, JSON.stringify(data));\n  return data;\n}\n```\n\n### Testing\n```javascript\n// Unit-Testing mit Jest\nconst request = require('supertest');\nconst app = require('../app');\n\ndescribe('Benutzer-API', () => {\n  test('GET /api/benutzer sollte Benutzer zurückgeben', async () => {\n    const response = await request(app)\n      .get('/api/benutzer')\n      .expect(200);\n    \n    expect(response.body).toHaveProperty('benutzer');\n    expect(Array.isArray(response.body.benutzer)).toBe(true);\n  });\n});\n```\n\n## Fazit\n\nNode.js bietet eine leistungsstarke Plattform für den Aufbau skalierbarer serverseitiger Anwendungen. Das Verständnis seiner Architektur und Prinzipien ist für Backend-Entwickler entscheidend.",
      "isNew": false,
      "isPopular": true,
      "createdAt": "2024-01-01T00:00:00Z"
    }
  ]
}
