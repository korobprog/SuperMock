{
  "profession": "frontend-developer",
  "language": "ru",
  "categories": [
    {
      "id": "interview-questions",
      "name": "Вопросы на собеседовании",
      "count": 156,
      "icon": "💬",
      "color": "bg-blue-100 text-blue-800"
    },
    {
      "id": "technical-tasks",
      "name": "Технические задания",
      "count": 89,
      "icon": "⚡",
      "color": "bg-green-100 text-green-800"
    },
    {
      "id": "system-design",
      "name": "Системный дизайн",
      "count": 34,
      "icon": "🏗️",
      "color": "bg-purple-100 text-purple-800"
    },
    {
      "id": "behavioral",
      "name": "Поведенческие вопросы",
      "count": 67,
      "icon": "🧠",
      "color": "bg-orange-100 text-orange-800"
    },
    {
      "id": "algorithms",
      "name": "Алгоритмы и структуры данных",
      "count": 123,
      "icon": "📊",
      "color": "bg-red-100 text-red-800"
    },
    {
      "id": "best-practices",
      "name": "Лучшие практики",
      "count": 78,
      "icon": "⭐",
      "color": "bg-yellow-100 text-yellow-800"
    }
  ],
  "materials": [
    {
      "id": 1,
      "title": "Топ-50 вопросов по JavaScript",
      "description": "Самые частые вопросы на собеседованиях по JavaScript с подробными ответами и примерами кода",
      "category": "interview-questions",
      "difficulty": "intermediate",
      "readTime": 15,
      "rating": 4.8,
      "reads": 1247,
      "tags": ["JavaScript", "Основы", "ES6+", "Интервью"],
      "content": "# Топ-50 вопросов по JavaScript\n\n## 1. Что такое hoisting в JavaScript?\n\nHoisting - это механизм JavaScript, при котором объявления переменных и функций поднимаются вверх своей области видимости.\n\n```javascript\nconsole.log(x); // undefined\nvar x = 5;\n\n// Интерпретируется как:\nvar x;\nconsole.log(x); // undefined\nx = 5;\n```\n\n## 2. Разница между var, let и const\n\n- **var**: функциональная область видимости, поднимается (hoisting)\n- **let**: блочная область видимости, не поднимается\n- **const**: блочная область видимости, не поднимается, нельзя переназначить\n\n```javascript\n// var - функциональная область видимости\nfunction example() {\n  var x = 1;\n  if (true) {\n    var x = 2; // та же переменная\n  }\n  console.log(x); // 2\n}\n\n// let - блочная область видимости\nfunction example2() {\n  let x = 1;\n  if (true) {\n    let x = 2; // новая переменная\n  }\n  console.log(x); // 1\n}\n```\n\n## 3. Что такое замыкания (closures)?\n\nЗамыкание - это функция, которая имеет доступ к переменным из внешней области видимости.\n\n```javascript\nfunction createCounter() {\n  let count = 0;\n  return function() {\n    return ++count;\n  };\n}\n\nconst counter = createCounter();\nconsole.log(counter()); // 1\nconsole.log(counter()); // 2\n```\n\n## 4. Event Loop в JavaScript\n\nEvent Loop - это механизм, который позволяет JavaScript выполнять асинхронные операции.\n\n```javascript\nconsole.log('1');\nsetTimeout(() => console.log('2'), 0);\nPromise.resolve().then(() => console.log('3'));\nconsole.log('4');\n\n// Вывод: 1, 4, 3, 2\n```\n\n## 5. Promise и async/await\n\n```javascript\n// Promise\nfunction fetchData() {\n  return new Promise((resolve, reject) => {\n    setTimeout(() => {\n      resolve('Данные получены');\n    }, 1000);\n  });\n}\n\n// async/await\nasync function getData() {\n  try {\n    const data = await fetchData();\n    console.log(data);\n  } catch (error) {\n    console.error(error);\n  }\n}\n```\n\n## 6. Прототипное наследование\n\n```javascript\nfunction Animal(name) {\n  this.name = name;\n}\n\nAnimal.prototype.speak = function() {\n  console.log(`${this.name} издает звук`);\n};\n\nfunction Dog(name) {\n  Animal.call(this, name);\n}\n\nDog.prototype = Object.create(Animal.prototype);\nDog.prototype.constructor = Dog;\n\nconst dog = new Dog('Рекс');\ndog.speak(); // Рекс издает звук\n```\n\n## 7. Map vs Object\n\n```javascript\n// Object\nconst obj = {\n  key: 'value'\n};\n\n// Map\nconst map = new Map();\nmap.set('key', 'value');\nmap.set(1, 'number key');\nmap.set({}, 'object key');\n\nconsole.log(map.get('key')); // value\nconsole.log(map.size); // 3\n```\n\n## 8. Деструктуризация\n\n```javascript\n// Массивы\nconst [a, b, ...rest] = [1, 2, 3, 4, 5];\nconsole.log(a, b, rest); // 1, 2, [3, 4, 5]\n\n// Объекты\nconst { name, age, ...other } = {\n  name: 'Иван',\n  age: 25,\n  city: 'Москва',\n  job: 'Developer'\n};\nconsole.log(name, age, other); // Иван, 25, {city: 'Москва', job: 'Developer'}\n```\n\n## 9. Модули ES6\n\n```javascript\n// math.js\nexport const add = (a, b) => a + b;\nexport const subtract = (a, b) => a - b;\nexport default function multiply(a, b) {\n  return a * b;\n}\n\n// main.js\nimport multiply, { add, subtract } from './math.js';\nconsole.log(add(2, 3)); // 5\nconsole.log(multiply(2, 3)); // 6\n```\n\n## 10. Работа с DOM\n\n```javascript\n// Создание элемента\nconst div = document.createElement('div');\ndiv.textContent = 'Привет, мир!';\ndiv.className = 'greeting';\n\ndocument.body.appendChild(div);\n\n// Поиск элементов\nconst element = document.querySelector('.greeting');\nconst elements = document.querySelectorAll('.item');\n\n// Обработчики событий\nconst button = document.querySelector('button');\nbutton.addEventListener('click', (event) => {\n  console.log('Кнопка нажата!');\n});\n```\n\nЭто базовые вопросы по JavaScript. Для полного понимания рекомендуется практиковаться с кодом и изучать документацию.",
      "isNew": false,
      "isPopular": true,
      "createdAt": "2024-01-01T00:00:00Z"
    },
    {
      "id": 2,
      "title": "React Hooks: полное руководство",
      "description": "Глубокий разбор всех хуков React с примерами использования и лучшими практиками",
      "category": "best-practices",
      "difficulty": "advanced",
      "readTime": 25,
      "rating": 4.9,
      "reads": 892,
      "tags": ["React", "Hooks", "Frontend", "useState", "useEffect"],
      "content": "# React Hooks: полное руководство\n\n## Введение\n\nReact Hooks были представлены в React 16.8 и позволяют использовать состояние и другие возможности React в функциональных компонентах.\n\n## useState\n\nБазовый хук для управления состоянием.\n\n```jsx\nimport React, { useState } from 'react';\n\nfunction Counter() {\n  const [count, setCount] = useState(0);\n  \n  return (\n    <div>\n      <p>Счетчик: {count}</p>\n      <button onClick={() => setCount(count + 1)}>\n        Увеличить\n      </button>\n    </div>\n  );\n}\n```\n\n### Функциональное обновление\n\n```jsx\nconst [count, setCount] = useState(0);\n\n// Вместо этого:\nsetCount(count + 1);\n\n// Используйте это:\nsetCount(prevCount => prevCount + 1);\n```\n\n## useEffect\n\nХук для выполнения побочных эффектов.\n\n```jsx\nimport React, { useState, useEffect } from 'react';\n\nfunction UserProfile({ userId }) {\n  const [user, setUser] = useState(null);\n  const [loading, setLoading] = useState(true);\n\n  useEffect(() => {\n    async function fetchUser() {\n      try {\n        const response = await fetch(`/api/users/${userId}`);\n        const userData = await response.json();\n        setUser(userData);\n      } catch (error) {\n        console.error('Ошибка загрузки пользователя:', error);\n      } finally {\n        setLoading(false);\n      }\n    }\n\n    fetchUser();\n  }, [userId]); // Зависимости\n\n  if (loading) return <div>Загрузка...</div>;\n  if (!user) return <div>Пользователь не найден</div>;\n\n  return (\n    <div>\n      <h2>{user.name}</h2>\n      <p>{user.email}</p>\n    </div>\n  );\n}\n```\n\n### Очистка эффектов\n\n```jsx\nuseEffect(() => {\n  const interval = setInterval(() => {\n    console.log('Тик');\n  }, 1000);\n\n  // Функция очистки\n  return () => {\n    clearInterval(interval);\n  };\n}, []);\n```\n\n## useContext\n\nХук для работы с контекстом.\n\n```jsx\n// Создание контекста\nconst ThemeContext = React.createContext();\n\n// Провайдер\nfunction App() {\n  const [theme, setTheme] = useState('light');\n\n  return (\n    <ThemeContext.Provider value={{ theme, setTheme }}>\n      <Header />\n      <Main />\n    </ThemeContext.Provider>\n  );\n}\n\n// Использование в компоненте\nfunction Header() {\n  const { theme, setTheme } = useContext(ThemeContext);\n\n  return (\n    <header className={`header-${theme}`}>\n      <button onClick={() => setTheme(theme === 'light' ? 'dark' : 'light')}>\n        Переключить тему\n      </button>\n    </header>\n  );\n}\n```\n\n## useReducer\n\nХук для управления сложным состоянием.\n\n```jsx\nimport React, { useReducer } from 'react';\n\nconst initialState = { count: 0 };\n\nfunction reducer(state, action) {\n  switch (action.type) {\n    case 'increment':\n      return { count: state.count + 1 };\n    case 'decrement':\n      return { count: state.count - 1 };\n    case 'reset':\n      return { count: 0 };\n    default:\n      throw new Error();\n  }\n}\n\nfunction Counter() {\n  const [state, dispatch] = useReducer(reducer, initialState);\n\n  return (\n    <div>\n      <p>Счетчик: {state.count}</p>\n      <button onClick={() => dispatch({ type: 'increment' })}>\n        +\n      </button>\n      <button onClick={() => dispatch({ type: 'decrement' })}>\n        -\n      </button>\n      <button onClick={() => dispatch({ type: 'reset' })}>\n        Сброс\n      </button>\n    </div>\n  );\n}\n```\n\n## useCallback\n\nХук для мемоизации функций.\n\n```jsx\nimport React, { useState, useCallback } from 'react';\n\nfunction Parent() {\n  const [count, setCount] = useState(0);\n\n  const handleClick = useCallback(() => {\n    console.log('Кнопка нажата');\n  }, []); // Зависимости\n\n  return (\n    <div>\n      <p>Счетчик: {count}</p>\n      <button onClick={() => setCount(count + 1)}>\n        Увеличить счетчик\n      </button>\n      <Child onClick={handleClick} />\n    </div>\n  );\n}\n\nconst Child = React.memo(function Child({ onClick }) {\n  console.log('Child перерендерился');\n  return <button onClick={onClick}>Дочерняя кнопка</button>;\n});\n```\n\n## useMemo\n\nХук для мемоизации значений.\n\n```jsx\nimport React, { useState, useMemo } from 'react';\n\nfunction ExpensiveComponent({ items }) {\n  const expensiveValue = useMemo(() => {\n    console.log('Вычисление дорогого значения');\n    return items.reduce((sum, item) => sum + item.value, 0);\n  }, [items]); // Зависимости\n\n  return <div>Результат: {expensiveValue}</div>;\n}\n```\n\n## useRef\n\nХук для создания изменяемых ссылок.\n\n```jsx\nimport React, { useRef, useEffect } from 'react';\n\nfunction TextInputWithFocusButton() {\n  const inputRef = useRef(null);\n\n  const focusInput = () => {\n    inputRef.current.focus();\n  };\n\n  return (\n    <div>\n      <input ref={inputRef} type=\"text\" />\n      <button onClick={focusInput}>Фокус на поле ввода</button>\n    </div>\n  );\n}\n\n// Сохранение предыдущего значения\nfunction usePrevious(value) {\n  const ref = useRef();\n  useEffect(() => {\n    ref.current = value;\n  });\n  return ref.current;\n}\n```\n\n## Кастомные хуки\n\n```jsx\n// useLocalStorage.js\nimport { useState, useEffect } from 'react';\n\nfunction useLocalStorage(key, initialValue) {\n  const [storedValue, setStoredValue] = useState(() => {\n    try {\n      const item = window.localStorage.getItem(key);\n      return item ? JSON.parse(item) : initialValue;\n    } catch (error) {\n      console.error(error);\n      return initialValue;\n    }\n  });\n\n  const setValue = value => {\n    try {\n      const valueToStore = value instanceof Function ? value(storedValue) : value;\n      setStoredValue(valueToStore);\n      window.localStorage.setItem(key, JSON.stringify(valueToStore));\n    } catch (error) {\n      console.error(error);\n    }\n  };\n\n  return [storedValue, setValue];\n}\n\n// Использование\nfunction App() {\n  const [name, setName] = useLocalStorage('name', 'Гость');\n\n  return (\n    <div>\n      <input\n        value={name}\n        onChange={e => setName(e.target.value)}\n        placeholder=\"Введите имя\"\n      />\n    </div>\n  );\n}\n```\n\n## Лучшие практики\n\n1. **Всегда указывайте зависимости в useEffect**\n2. **Используйте функциональные обновления в useState**\n3. **Мемоизируйте дорогие вычисления с useMemo**\n4. **Мемоизируйте функции с useCallback**\n5. **Создавайте кастомные хуки для переиспользуемой логики**\n6. **Избегайте создания объектов в рендере**\n\n## Заключение\n\nReact Hooks предоставляют мощный и гибкий способ управления состоянием и побочными эффектами в функциональных компонентах. Правильное использование хуков приводит к более чистому и поддерживаемому коду.",
      "isNew": true,
      "isPopular": true,
      "createdAt": "2024-01-15T00:00:00Z"
    },
    {
      "id": 3,
      "title": "TypeScript: продвинутые типы",
      "description": "Углубленное изучение системы типов TypeScript с практическими примерами",
      "category": "interview-questions",
      "difficulty": "advanced",
      "readTime": 18,
      "rating": 4.7,
      "reads": 234,
      "tags": ["TypeScript", "Типизация", "Frontend", "Дженерики"],
      "content": "# TypeScript: продвинутые типы\n\n## Введение\n\nTypeScript предоставляет мощную систему типов, которая помогает писать более надежный код. Рассмотрим продвинутые возможности типизации.\n\n## Условные типы (Conditional Types)\n\n```typescript\ntype NonNullable<T> = T extends null | undefined ? never : T;\n\ntype T0 = NonNullable<string | number | null>; // string | number\ntype T1 = NonNullable<string[] | null | undefined>; // string[]\n```\n\n## Маппинг типов (Mapped Types)\n\n```typescript\n// Сделать все свойства опциональными\ntype Partial<T> = {\n  [P in keyof T]?: T[P];\n};\n\n// Сделать все свойства обязательными\ntype Required<T> = {\n  [P in keyof T]-?: T[P];\n};\n\n// Сделать все свойства только для чтения\ntype Readonly<T> = {\n  readonly [P in keyof T]: T[P];\n};\n\n// Пример использования\ninterface User {\n  id: number;\n  name: string;\n  email?: string;\n}\n\ntype PartialUser = Partial<User>;\n// { id?: number; name?: string; email?: string }\n\ntype RequiredUser = Required<User>;\n// { id: number; name: string; email: string }\n```\n\n## Дженерики (Generics)\n\n```typescript\n// Простой дженерик\nfunction identity<T>(arg: T): T {\n  return arg;\n}\n\nconst result = identity<string>('hello');\nconst result2 = identity('hello'); // автоматический вывод типа\n\n// Дженерик с ограничениями\ninterface Lengthwise {\n  length: number;\n}\n\nfunction loggingIdentity<T extends Lengthwise>(arg: T): T {\n  console.log(arg.length);\n  return arg;\n}\n\nloggingIdentity('hello'); // OK\nloggingIdentity([1, 2, 3]); // OK\n// loggingIdentity(123); // Error\n```\n\n## Утилитарные типы\n\n```typescript\n// Pick - выбрать определенные свойства\ninterface Todo {\n  title: string;\n  description: string;\n  completed: boolean;\n}\n\ntype TodoPreview = Pick<Todo, 'title' | 'completed'>;\n// { title: string; completed: boolean }\n\n// Omit - исключить определенные свойства\ntype TodoInfo = Omit<Todo, 'completed'>;\n// { title: string; description: string }\n\n// Record - создать объект с определенными ключами и значениями\ntype CatInfo = {\n  age: number;\n  breed: string;\n};\n\ntype CatName = 'miffy' | 'boris' | 'mordred';\n\nconst cats: Record<CatName, CatInfo> = {\n  miffy: { age: 10, breed: 'Persian' },\n  boris: { age: 5, breed: 'Maine Coon' },\n  mordred: { age: 16, breed: 'British Shorthair' }\n};\n```\n\n## Пересечения и объединения типов\n\n```typescript\n// Объединение типов\ntype Status = 'loading' | 'success' | 'error';\n\ntype ApiResponse<T> = {\n  data: T;\n  status: Status;\n};\n\n// Пересечение типов\ninterface HasName {\n  name: string;\n}\n\ninterface HasAge {\n  age: number;\n}\n\ntype Person = HasName & HasAge;\n// { name: string; age: number }\n\n// Условные типы с объединениями\ntype ArrayElement<T> = T extends Array<infer U> ? U : never;\n\ntype T0 = ArrayElement<string[]>; // string\ntype T1 = ArrayElement<number[]>; // number\n```\n\n## Инференция типов\n\n```typescript\n// Автоматический вывод типа\nconst names = ['Alice', 'Bob', 'Charlie'];\n// TypeScript автоматически выводит тип как string[]\n\n// Вывод типа в функциях\nfunction map<T, U>(array: T[], fn: (item: T) => U): U[] {\n  return array.map(fn);\n}\n\nconst numbers = [1, 2, 3, 4];\nconst doubled = map(numbers, x => x * 2); // number[]\nconst strings = map(numbers, x => x.toString()); // string[]\n```\n\n## Template Literal Types\n\n```typescript\ntype EmailLocaleIDs = 'welcome_email' | 'email_heading';\ntype FooterLocaleIDs = 'footer_title' | 'footer_sendoff';\n\ntype AllLocaleIDs = `${EmailLocaleIDs | FooterLocaleIDs}_id`;\n// 'welcome_email_id' | 'email_heading_id' | 'footer_title_id' | 'footer_sendoff_id'\n\ntype Lang = 'en' | 'ru';\ntype LocaleMessageIDs = `${Lang}_${AllLocaleIDs}`;\n// 'en_welcome_email_id' | 'en_email_heading_id' | ... | 'ru_footer_sendoff_id'\n```\n\n## Типы для React компонентов\n\n```typescript\nimport React from 'react';\n\n// Типизация пропсов\ninterface ButtonProps {\n  children: React.ReactNode;\n  onClick?: () => void;\n  variant?: 'primary' | 'secondary' | 'danger';\n  size?: 'small' | 'medium' | 'large';\n  disabled?: boolean;\n}\n\nconst Button: React.FC<ButtonProps> = ({\n  children,\n  onClick,\n  variant = 'primary',\n  size = 'medium',\n  disabled = false\n}) => {\n  return (\n    <button\n      onClick={onClick}\n      className={`btn btn-${variant} btn-${size}`}\n      disabled={disabled}\n    >\n      {children}\n    </button>\n  );\n};\n\n// Типизация хуков\nfunction useLocalStorage<T>(key: string, initialValue: T) {\n  const [storedValue, setStoredValue] = React.useState<T>(() => {\n    try {\n      const item = window.localStorage.getItem(key);\n      return item ? JSON.parse(item) : initialValue;\n    } catch (error) {\n      return initialValue;\n    }\n  });\n\n  const setValue = (value: T | ((val: T) => T)) => {\n    try {\n      const valueToStore = value instanceof Function ? value(storedValue) : value;\n      setStoredValue(valueToStore);\n      window.localStorage.setItem(key, JSON.stringify(valueToStore));\n    } catch (error) {\n      console.error(error);\n    }\n  };\n\n  return [storedValue, setValue] as const;\n}\n```\n\n## Типы для API\n\n```typescript\n// Типы для API ответов\ninterface ApiResponse<T> {\n  data: T;\n  status: 'success' | 'error';\n  message?: string;\n}\n\ninterface User {\n  id: number;\n  name: string;\n  email: string;\n}\n\n// Типизированная функция API\nasync function fetchUser(id: number): Promise<ApiResponse<User>> {\n  const response = await fetch(`/api/users/${id}`);\n  return response.json();\n}\n\n// Использование\nconst userResponse = await fetchUser(1);\nif (userResponse.status === 'success') {\n  console.log(userResponse.data.name); // TypeScript знает, что это User\n}\n```\n\n## Заключение\n\nПродвинутые типы TypeScript позволяют создавать более выразительные и безопасные типы. Правильное использование этих возможностей приводит к лучшему качеству кода и уменьшению количества ошибок во время выполнения.",
      "isNew": true,
      "isPopular": false,
      "createdAt": "2024-01-20T00:00:00Z"
    }
  ]
}
