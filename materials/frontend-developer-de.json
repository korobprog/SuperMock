{
  "profession": "frontend-developer",
  "language": "de",
  "categories": [
    {
      "id": "interview-questions",
      "name": "Vorstellungsgespräch Fragen",
      "count": 156,
      "icon": "💬",
      "color": "bg-blue-100 text-blue-800"
    },
    {
      "id": "technical-tasks",
      "name": "Technische Aufgaben",
      "count": 89,
      "icon": "⚡",
      "color": "bg-green-100 text-green-800"
    },
    {
      "id": "system-design",
      "name": "Systemdesign",
      "count": 34,
      "icon": "🏗️",
      "color": "bg-purple-100 text-purple-800"
    },
    {
      "id": "behavioral",
      "name": "Verhaltensfragen",
      "count": 67,
      "icon": "🧠",
      "color": "bg-orange-100 text-orange-800"
    },
    {
      "id": "algorithms",
      "name": "Algorithmen und Datenstrukturen",
      "count": 123,
      "icon": "📊",
      "color": "bg-red-100 text-red-800"
    },
    {
      "id": "best-practices",
      "name": "Beste Praktiken",
      "count": 78,
      "icon": "⭐",
      "color": "bg-yellow-100 text-yellow-800"
    }
  ],
  "materials": [
    {
      "id": 1,
      "title": "Top 50 JavaScript Fragen",
      "description": "Die häufigsten Fragen in JavaScript-Vorstellungsgesprächen mit detaillierten Antworten und Code-Beispielen",
      "category": "interview-questions",
      "difficulty": "intermediate",
      "readTime": 15,
      "rating": 4.8,
      "reads": 1247,
      "tags": ["JavaScript", "Grundlagen", "ES6+", "Interview"],
      "content": "# Top 50 JavaScript Fragen\n\n## 1. Was ist Hoisting in JavaScript?\n\nHoisting ist ein Mechanismus in JavaScript, bei dem Variablen- und Funktionsdeklarationen an den Anfang ihres Gültigkeitsbereichs verschoben werden.\n\n```javascript\nconsole.log(x); // undefined\nvar x = 5;\n\n// Wird interpretiert als:\nvar x;\nconsole.log(x); // undefined\nx = 5;\n```\n\n## 2. Unterschied zwischen var, let und const\n\n- **var**: Funktionsbereich, wird gehoisted\n- **let**: Blockbereich, wird nicht gehoisted\n- **const**: Blockbereich, wird nicht gehoisted, kann nicht neu zugewiesen werden\n\n```javascript\n// var - Funktionsbereich\nfunction example() {\n  var x = 1;\n  if (true) {\n    var x = 2; // dieselbe Variable\n  }\n  console.log(x); // 2\n}\n\n// let - Blockbereich\nfunction example2() {\n  let x = 1;\n  if (true) {\n    let x = 2; // neue Variable\n  }\n  console.log(x); // 1\n}\n```\n\n## 3. Was sind Closures?\n\nEine Closure ist eine Funktion, die Zugriff auf Variablen aus dem äußeren Gültigkeitsbereich hat.\n\n```javascript\nfunction createCounter() {\n  let count = 0;\n  return function() {\n    return ++count;\n  };\n}\n\nconst counter = createCounter();\nconsole.log(counter()); // 1\nconsole.log(counter()); // 2\n```\n\n## 4. Event Loop in JavaScript\n\nEvent Loop ist ein Mechanismus, der JavaScript ermöglicht, asynchrone Operationen auszuführen.\n\n```javascript\nconsole.log('1');\nsetTimeout(() => console.log('2'), 0);\nPromise.resolve().then(() => console.log('3'));\nconsole.log('4');\n\n// Ausgabe: 1, 4, 3, 2\n```\n\n## 5. Promise und async/await\n\n```javascript\n// Promise\nfunction fetchData() {\n  return new Promise((resolve, reject) => {\n    setTimeout(() => {\n      resolve('Daten erhalten');\n    }, 1000);\n  });\n}\n\n// async/await\nasync function getData() {\n  try {\n    const data = await fetchData();\n    console.log(data);\n  } catch (error) {\n    console.error(error);\n  }\n}\n```\n\n## 6. Prototypische Vererbung\n\n```javascript\nfunction Animal(name) {\n  this.name = name;\n}\n\nAnimal.prototype.speak = function() {\n  console.log(`${this.name} macht ein Geräusch`);\n};\n\nfunction Dog(name) {\n  Animal.call(this, name);\n}\n\nDog.prototype = Object.create(Animal.prototype);\nDog.prototype.constructor = Dog;\n\nconst dog = new Dog('Rex');\ndog.speak(); // Rex macht ein Geräusch\n```\n\n## 7. Map vs Object\n\n```javascript\n// Object\nconst obj = {\n  key: 'value'\n};\n\n// Map\nconst map = new Map();\nmap.set('key', 'value');\nmap.set(1, 'number key');\nmap.set({}, 'object key');\n\nconsole.log(map.get('key')); // value\nconsole.log(map.size); // 3\n```\n\n## 8. Destrukturierung\n\n```javascript\n// Arrays\nconst [a, b, ...rest] = [1, 2, 3, 4, 5];\nconsole.log(a, b, rest); // 1, 2, [3, 4, 5]\n\n// Objekte\nconst { name, age, ...other } = {\n  name: 'Hans',\n  age: 25,\n  city: 'Berlin',\n  job: 'Developer'\n};\nconsole.log(name, age, other); // Hans, 25, {city: 'Berlin', job: 'Developer'}\n```\n\n## 9. ES6 Module\n\n```javascript\n// math.js\nexport const add = (a, b) => a + b;\nexport const subtract = (a, b) => a - b;\nexport default function multiply(a, b) {\n  return a * b;\n}\n\n// main.js\nimport multiply, { add, subtract } from './math.js';\nconsole.log(add(2, 3)); // 5\nconsole.log(multiply(2, 3)); // 6\n```\n\n## 10. DOM-Manipulation\n\n```javascript\n// Element erstellen\nconst div = document.createElement('div');\ndiv.textContent = 'Hallo Welt!';\ndiv.className = 'greeting';\n\ndocument.body.appendChild(div);\n\n// Elemente finden\nconst element = document.querySelector('.greeting');\nconst elements = document.querySelectorAll('.item');\n\n// Event-Handler\nconst button = document.querySelector('button');\nbutton.addEventListener('click', (event) => {\n  console.log('Button geklickt!');\n});\n```\n\nDies sind die grundlegenden JavaScript-Fragen. Für ein vollständiges Verständnis wird empfohlen, mit Code zu üben und die Dokumentation zu studieren.",
      "isNew": false,
      "isPopular": true,
      "createdAt": "2024-01-01T00:00:00Z"
    },
    {
      "id": 2,
      "title": "React Hooks: Vollständige Anleitung",
      "description": "Tiefgehende Analyse aller React Hooks mit Verwendungsbeispielen und Best Practices",
      "category": "best-practices",
      "difficulty": "advanced",
      "readTime": 25,
      "rating": 4.9,
      "reads": 892,
      "tags": ["React", "Hooks", "Frontend", "useState", "useEffect"],
      "content": "# React Hooks: Vollständige Anleitung\n\n## Einführung\n\nReact Hooks wurden in React 16.8 eingeführt und ermöglichen es, State und andere React-Features in Funktionskomponenten zu verwenden.\n\n## useState\n\nBasis-Hook für State-Management.\n\n```jsx\nimport React, { useState } from 'react';\n\nfunction Counter() {\n  const [count, setCount] = useState(0);\n  \n  return (\n    <div>\n      <p>Zähler: {count}</p>\n      <button onClick={() => setCount(count + 1)}>\n        Erhöhen\n      </button>\n    </div>\n  );\n}\n```\n\n### Funktionale Updates\n\n```jsx\nconst [count, setCount] = useState(0);\n\n// Anstatt dies:\nsetCount(count + 1);\n\n// Verwende dies:\nsetCount(prevCount => prevCount + 1);\n```\n\n## useEffect\n\nHook für Seiteneffekte.\n\n```jsx\nimport React, { useState, useEffect } from 'react';\n\nfunction UserProfile({ userId }) {\n  const [user, setUser] = useState(null);\n  const [loading, setLoading] = useState(true);\n\n  useEffect(() => {\n    async function fetchUser() {\n      try {\n        const response = await fetch(`/api/users/${userId}`);\n        const userData = await response.json();\n        setUser(userData);\n      } catch (error) {\n        console.error('Fehler beim Laden des Benutzers:', error);\n      } finally {\n        setLoading(false);\n      }\n    }\n\n    fetchUser();\n  }, [userId]); // Abhängigkeiten\n\n  if (loading) return <div>Lädt...</div>;\n  if (!user) return <div>Benutzer nicht gefunden</div>;\n\n  return (\n    <div>\n      <h2>{user.name}</h2>\n      <p>{user.email}</p>\n    </div>\n  );\n}\n```\n\n### Cleanup von Effekten\n\n```jsx\nuseEffect(() => {\n  const interval = setInterval(() => {\n    console.log('Tick');\n  }, 1000);\n\n  // Cleanup-Funktion\n  return () => {\n    clearInterval(interval);\n  };\n}, []);\n```\n\n## useContext\n\nHook für Context-Arbeit.\n\n```jsx\n// Context erstellen\nconst ThemeContext = React.createContext();\n\n// Provider\nfunction App() {\n  const [theme, setTheme] = useState('light');\n\n  return (\n    <ThemeContext.Provider value={{ theme, setTheme }}>\n      <Header />\n      <Main />\n    </ThemeContext.Provider>\n  );\n}\n\n// Verwendung in Komponente\nfunction Header() {\n  const { theme, setTheme } = useContext(ThemeContext);\n\n  return (\n    <header className={`header-${theme}`}>\n      <button onClick={() => setTheme(theme === 'light' ? 'dark' : 'light')}>\n        Theme wechseln\n      </button>\n    </header>\n  );\n}\n```\n\n## useReducer\n\nHook für komplexes State-Management.\n\n```jsx\nimport React, { useReducer } from 'react';\n\nconst initialState = { count: 0 };\n\nfunction reducer(state, action) {\n  switch (action.type) {\n    case 'increment':\n      return { count: state.count + 1 };\n    case 'decrement':\n      return { count: state.count - 1 };\n    case 'reset':\n      return { count: 0 };\n    default:\n      throw new Error();\n  }\n}\n\nfunction Counter() {\n  const [state, dispatch] = useReducer(reducer, initialState);\n\n  return (\n    <div>\n      <p>Zähler: {state.count}</p>\n      <button onClick={() => dispatch({ type: 'increment' })}>\n        +\n      </button>\n      <button onClick={() => dispatch({ type: 'decrement' })}>\n        -\n      </button>\n      <button onClick={() => dispatch({ type: 'reset' })}>\n        Reset\n      </button>\n    </div>\n  );\n}\n```\n\n## useCallback\n\nHook für Funktions-Memoization.\n\n```jsx\nimport React, { useState, useCallback } from 'react';\n\nfunction Parent() {\n  const [count, setCount] = useState(0);\n\n  const handleClick = useCallback(() => {\n    console.log('Button geklickt');\n  }, []); // Abhängigkeiten\n\n  return (\n    <div>\n      <p>Zähler: {count}</p>\n      <button onClick={() => setCount(count + 1)}>\n        Zähler erhöhen\n      </button>\n      <Child onClick={handleClick} />\n    </div>\n  );\n}\n\nconst Child = React.memo(function Child({ onClick }) {\n  console.log('Child neu gerendert');\n  return <button onClick={onClick}>Kind-Button</button>;\n});\n```\n\n## useMemo\n\nHook für Wert-Memoization.\n\n```jsx\nimport React, { useState, useMemo } from 'react';\n\nfunction ExpensiveComponent({ items }) {\n  const expensiveValue = useMemo(() => {\n    console.log('Teurer Wert berechnet');\n    return items.reduce((sum, item) => sum + item.value, 0);\n  }, [items]); // Abhängigkeiten\n\n  return <div>Ergebnis: {expensiveValue}</div>;\n}\n```\n\n## useRef\n\nHook für mutable Referenzen.\n\n```jsx\nimport React, { useRef, useEffect } from 'react';\n\nfunction TextInputWithFocusButton() {\n  const inputRef = useRef(null);\n\n  const focusInput = () => {\n    inputRef.current.focus();\n  };\n\n  return (\n    <div>\n      <input ref={inputRef} type=\"text\" />\n      <button onClick={focusInput}>Eingabefeld fokussieren</button>\n    </div>\n  );\n}\n\n// Vorherigen Wert speichern\nfunction usePrevious(value) {\n  const ref = useRef();\n  useEffect(() => {\n    ref.current = value;\n  });\n  return ref.current;\n}\n```\n\n## Custom Hooks\n\n```jsx\n// useLocalStorage.js\nimport { useState, useEffect } from 'react';\n\nfunction useLocalStorage(key, initialValue) {\n  const [storedValue, setStoredValue] = useState(() => {\n    try {\n      const item = window.localStorage.getItem(key);\n      return item ? JSON.parse(item) : initialValue;\n    } catch (error) {\n      console.error(error);\n      return initialValue;\n    }\n  });\n\n  const setValue = value => {\n    try {\n      const valueToStore = value instanceof Function ? value(storedValue) : value;\n      setStoredValue(valueToStore);\n      window.localStorage.setItem(key, JSON.stringify(valueToStore));\n    } catch (error) {\n      console.error(error);\n    }\n  };\n\n  return [storedValue, setValue];\n}\n\n// Verwendung\nfunction App() {\n  const [name, setName] = useLocalStorage('name', 'Gast');\n\n  return (\n    <div>\n      <input\n        value={name}\n        onChange={e => setName(e.target.value)}\n        placeholder=\"Name eingeben\"\n      />\n    </div>\n  );\n}\n```\n\n## Best Practices\n\n1. **Immer Abhängigkeiten in useEffect angeben**\n2. **Funktionale Updates in useState verwenden**\n3. **Teure Berechnungen mit useMemo memoizieren**\n4. **Funktionen mit useCallback memoizieren**\n5. **Custom Hooks für wiederverwendbare Logik erstellen**\n6. **Objekte im Render vermeiden**\n\n## Fazit\n\nReact Hooks bieten eine mächtige und flexible Möglichkeit, State und Seiteneffekte in Funktionskomponenten zu verwalten. Die richtige Verwendung von Hooks führt zu saubererem und wartbarerem Code.",
      "isNew": true,
      "isPopular": true,
      "createdAt": "2024-01-15T00:00:00Z"
    },
    {
      "id": 3,
      "title": "TypeScript: Erweiterte Typen",
      "description": "Vertiefte Studie des TypeScript-Typsystems mit praktischen Beispielen",
      "category": "interview-questions",
      "difficulty": "advanced",
      "readTime": 18,
      "rating": 4.7,
      "reads": 234,
      "tags": ["TypeScript", "Typisierung", "Frontend", "Generics"],
      "content": "# TypeScript: Erweiterte Typen\n\n## Einführung\n\nTypeScript bietet ein mächtiges Typsystem, das hilft, robusteren Code zu schreiben. Schauen wir uns die erweiterten Typisierungsmöglichkeiten an.\n\n## Bedingte Typen (Conditional Types)\n\n```typescript\ntype NonNullable<T> = T extends null | undefined ? never : T;\n\ntype T0 = NonNullable<string | number | null>; // string | number\ntype T1 = NonNullable<string[] | null | undefined>; // string[]\n```\n\n## Mapped Types\n\n```typescript\n// Alle Eigenschaften optional machen\ntype Partial<T> = {\n  [P in keyof T]?: T[P];\n};\n\n// Alle Eigenschaften erforderlich machen\ntype Required<T> = {\n  [P in keyof T]-?: T[P];\n};\n\n// Alle Eigenschaften schreibgeschützt machen\ntype Readonly<T> = {\n  readonly [P in keyof T]: T[P];\n};\n\n// Verwendungsbeispiel\ninterface User {\n  id: number;\n  name: string;\n  email?: string;\n}\n\ntype PartialUser = Partial<User>;\n// { id?: number; name?: string; email?: string }\n\ntype RequiredUser = Required<User>;\n// { id: number; name: string; email: string }\n```\n\n## Generics\n\n```typescript\n// Einfaches Generic\nfunction identity<T>(arg: T): T {\n  return arg;\n}\n\nconst result = identity<string>('hello');\nconst result2 = identity('hello'); // automatische Typinferenz\n\n// Generic mit Einschränkungen\ninterface Lengthwise {\n  length: number;\n}\n\nfunction loggingIdentity<T extends Lengthwise>(arg: T): T {\n  console.log(arg.length);\n  return arg;\n}\n\nloggingIdentity('hello'); // OK\nloggingIdentity([1, 2, 3]); // OK\n// loggingIdentity(123); // Fehler\n```\n\n## Utility Types\n\n```typescript\n// Pick - bestimmte Eigenschaften auswählen\ninterface Todo {\n  title: string;\n  description: string;\n  completed: boolean;\n}\n\ntype TodoPreview = Pick<Todo, 'title' | 'completed'>;\n// { title: string; completed: boolean }\n\n// Omit - bestimmte Eigenschaften ausschließen\ntype TodoInfo = Omit<Todo, 'completed'>;\n// { title: string; description: string }\n\n// Record - Objekt mit bestimmten Schlüsseln und Werten erstellen\ntype CatInfo = {\n  age: number;\n  breed: string;\n};\n\ntype CatName = 'miffy' | 'boris' | 'mordred';\n\nconst cats: Record<CatName, CatInfo> = {\n  miffy: { age: 10, breed: 'Persian' },\n  boris: { age: 5, breed: 'Maine Coon' },\n  mordred: { age: 16, breed: 'British Shorthair' }\n};\n```\n\n## Schnitt- und Vereinigungsmengen von Typen\n\n```typescript\n// Typvereinigung\ntype Status = 'loading' | 'success' | 'error';\n\ntype ApiResponse<T> = {\n  data: T;\n  status: Status;\n};\n\n// Typschnittmenge\ninterface HasName {\n  name: string;\n}\n\ninterface HasAge {\n  age: number;\n}\n\ntype Person = HasName & HasAge;\n// { name: string; age: number }\n\n// Bedingte Typen mit Vereinigungen\ntype ArrayElement<T> = T extends Array<infer U> ? U : never;\n\ntype T0 = ArrayElement<string[]>; // string\ntype T1 = ArrayElement<number[]>; // number\n```\n\n## Typinferenz\n\n```typescript\n// Automatische Typinferenz\nconst names = ['Alice', 'Bob', 'Charlie'];\n// TypeScript leitet automatisch den Typ als string[] ab\n\n// Typinferenz in Funktionen\nfunction map<T, U>(array: T[], fn: (item: T) => U): U[] {\n  return array.map(fn);\n}\n\nconst numbers = [1, 2, 3, 4];\nconst doubled = map(numbers, x => x * 2); // number[]\nconst strings = map(numbers, x => x.toString()); // string[]\n```\n\n## Template Literal Types\n\n```typescript\ntype EmailLocaleIDs = 'welcome_email' | 'email_heading';\ntype FooterLocaleIDs = 'footer_title' | 'footer_sendoff';\n\ntype AllLocaleIDs = `${EmailLocaleIDs | FooterLocaleIDs}_id`;\n// 'welcome_email_id' | 'email_heading_id' | 'footer_title_id' | 'footer_sendoff_id'\n\ntype Lang = 'en' | 'de';\ntype LocaleMessageIDs = `${Lang}_${AllLocaleIDs}`;\n// 'en_welcome_email_id' | 'en_email_heading_id' | ... | 'de_footer_sendoff_id'\n```\n\n## Typen für React-Komponenten\n\n```typescript\nimport React from 'react';\n\n// Props-Typisierung\ninterface ButtonProps {\n  children: React.ReactNode;\n  onClick?: () => void;\n  variant?: 'primary' | 'secondary' | 'danger';\n  size?: 'small' | 'medium' | 'large';\n  disabled?: boolean;\n}\n\nconst Button: React.FC<ButtonProps> = ({\n  children,\n  onClick,\n  variant = 'primary',\n  size = 'medium',\n  disabled = false\n}) => {\n  return (\n    <button\n      onClick={onClick}\n      className={`btn btn-${variant} btn-${size}`}\n      disabled={disabled}\n    >\n      {children}\n    </button>\n  );\n};\n\n// Hook-Typisierung\nfunction useLocalStorage<T>(key: string, initialValue: T) {\n  const [storedValue, setStoredValue] = React.useState<T>(() => {\n    try {\n      const item = window.localStorage.getItem(key);\n      return item ? JSON.parse(item) : initialValue;\n    } catch (error) {\n      return initialValue;\n    }\n  });\n\n  const setValue = (value: T | ((val: T) => T)) => {\n    try {\n      const valueToStore = value instanceof Function ? value(storedValue) : value;\n      setStoredValue(valueToStore);\n      window.localStorage.setItem(key, JSON.stringify(valueToStore));\n    } catch (error) {\n      console.error(error);\n    }\n  };\n\n  return [storedValue, setValue] as const;\n}\n```\n\n## Typen für APIs\n\n```typescript\n// Typen für API-Antworten\ninterface ApiResponse<T> {\n  data: T;\n  status: 'success' | 'error';\n  message?: string;\n}\n\ninterface User {\n  id: number;\n  name: string;\n  email: string;\n}\n\n// Typisierte API-Funktion\nasync function fetchUser(id: number): Promise<ApiResponse<User>> {\n  const response = await fetch(`/api/users/${id}`);\n  return response.json();\n}\n\n// Verwendung\nconst userResponse = await fetchUser(1);\nif (userResponse.status === 'success') {\n  console.log(userResponse.data.name); // TypeScript weiß, dass es User ist\n}\n```\n\n## Fazit\n\nErweiterte TypeScript-Typen ermöglichen es, ausdrucksstärkere und sicherere Typen zu erstellen. Die richtige Verwendung dieser Fähigkeiten führt zu besserer Codequalität und weniger Laufzeitfehlern.",
      "isNew": true,
      "isPopular": false,
      "createdAt": "2024-01-20T00:00:00Z"
    }
  ]
}
