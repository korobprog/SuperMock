# Simple Dockerfile that fixes missing dependencies
FROM mockmate-backend

# Install missing dependencies
WORKDIR /app/backend
RUN npm install jsonwebtoken @types/jsonwebtoken

# Run the production server
CMD ["node", "server/index.mjs"]